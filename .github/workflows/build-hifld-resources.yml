name: Build HIFLD Texas Resources

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 1 * *'

permissions:
  contents: write

jobs:
  build-hifld-resources:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Ensure Texas county polygons exist
        run: |
          if [ ! -f public/data/geo/texas_counties.geojson ]; then
            node scripts/fetchTexasCountyGeojson.js
          fi

      - name: Build HIFLD Texas resources
        run: node scripts/fetchHifldTexasResources.js

      - name: Commit generated HIFLD resource data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: Build HIFLD Texas resources
          file_pattern: public/data/resources/*.geojson public/data/texas_county_resources.json
